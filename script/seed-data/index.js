const seedUsers = require('./user-seed')
const seedBoards = require('./board-seed')
const seedTasks = require('./task-seed')
const seedChecklistItems = require('./checklist-item-seed')
const seedMantra = require('./mantra-seed')

module.exports = {
  seedUsers,
  seedBoards,
  seedTasks,
  seedChecklistItems,
  seedMantra
}
